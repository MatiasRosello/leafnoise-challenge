apiVersion: v1
kind: Service
metadata:
  name: angular-service
  namespace: leafnoise
spec:
  selector:
    app: angular
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort
